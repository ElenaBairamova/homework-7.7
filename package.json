{
  "name": "cypress2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:main": "npx cypress run --record --group admin --spec \"cypress/e2e/adminPage.cy.js\" --ci-build-id 4",
    "test:admin": "npx cypress run --record --group main --spec \"cypress/e2e/mainPage.cy.js\" --ci-build-id 4",
    "test:parallel": "run-p -c test:main test:admin"

    
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^13.6.2"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
